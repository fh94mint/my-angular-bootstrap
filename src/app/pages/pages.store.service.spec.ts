import { PagesStoreService } from './pages.store.service';

describe('PagesStoreService', () => {
  it('should be created', () => {
    const pagesStoreService = new PagesStoreService();
    expect(pagesStoreService).toBeTruthy();
  });
});
